package main

import (
	"C"
)

//export funct
func funct() int {
	/**
	dir := sp.NAMED
	sc, err := sigmaclnt.NewSigmaClnt(proc.GetProcEnv())

	if err != nil {
		return -1
	}

	sts, err := sc.GetDir(dir)

	if err != nil {
		return -1
	}

	println("%v: %v\n", dir, sp.Names(sts))*/
	return 200
}

func main() {}
